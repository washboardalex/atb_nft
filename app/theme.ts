import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "GT America Expanded Black",
    body: "GT America Extended Regular",
  },
});

export default theme;
